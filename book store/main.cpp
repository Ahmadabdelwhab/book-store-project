#include <iostream>
#include <cstring>
#include "book_class.h"
#include <string>
#include "managment.h"
#include <fstream>


//write.write( studentPassword.c_str(), sizeof(char)*studentPassword.size() );
using namespace std;

int main()
{
    
    searchMenu("tes.txt");
    // fstream in("tes.txt",ios::in | ios::out);
    // addBook("tes.txt");
    

    
    // in.close();
    // book::printBook(b);
}
